# Release ledger — JezPress Woo Brand Categories

**Slug:** `jezpress-woo-brand-categories`

> Dev-only, append-only **governance log**. Excluded from the release ZIP (the whole `.claude/` folder is excluded). One row per shipped version, **newest first**, appended at Phase 6 of every release once the pre-release audit verdict is known and the dashboard patches are confirmed.
>
> **Why it exists:** the `Audit` and `JezPress`/`Dashboard` columns are the point — they are the only durable record that the audit gate ran and the ship steps completed (git tags + `readme.txt` don't capture that). `Version`/`Date`/`Shipped` are just anchors that also live in git + `readme.txt`; keep `Shipped` to **one line** — do not reproduce the changelog here.

## Audit column values
- `clean` — pre-release audit found no Critical/High.
- `fixes (N H)` / `fixes (N C)` — N High/Critical found and fixed before the tag; re-audited if material.
- `blocked → fixed` — a Critical was present; shipped only after it was resolved (note what in the row).
- `manual` — subagents unavailable, manual self-review only (per `audit-guide.md` — say so explicitly).

## JezPress / Dashboard column values
- **JezPress**: `uploaded` · `skipped` (no token) · `n/a`
- **Dashboard**: `patched` (description + changelog tabs) · `pending` · `n/a`

## Ledger
| Version | Date | Audit | Shipped | JezPress | Dashboard |
|---|---|---|---|---|---|
| 1.16.0 | 2026-07-05 | manual | Filter-bar UX polish + AJAX robustness (post-1.15 refinements): accordion (one dropdown open at a time via toggle event); AJAX mode hides redundant facet-Apply + sort-Go buttons (apply on change) via a jpwbc-ajax-active class JS adds only when AJAX inits (no-JS keeps buttons); price no longer auto-applies on blur (change-to-apply scoped to select/checkbox); AJAX grid selector falls back ul.products -> Elementor Products widget -> .woocommerce ul.products -> .products (confirmed Birch archive tmpl 3529 uses Elementor woocommerce-products => ul.products, so AJAX now engages; prior "not working" = stale cached JS). Cache tab: attribute index live %+<progress> bar. JS/CSS + small admin progress readout (wp_count_posts, esc_attr int) - no new attack surface; light diff-review. CI registered 1.16.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.15.0 | 2026-07-05 | manual | AJAX filtering for the Brand Filter Bar (Radley/Myer-style, no reload): on category/price/sort/facet change, fetch the same filtered URL, swap the product grid (configurable selector, default ul.products) + pagination + result count + the filter bar (live counts/active), pushState URL sync, popstate back-button, loading spinner, smooth scroll; graceful fallback to full-page nav (no-JS or grid not found). Widget: AJAX toggle + Product-grid CSS selector; [jpwbc_brand_filter] +ajax/results atts. JS-only enhancement + 2 esc_attr'd data attributes - no new server/query/output surface (DOMParser doesn't exec scripts; swapped markup server-rendered). Light diff-review (per audit-guide). CI registered 1.15.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.14.0 | 2026-07-05 | clean | Phase 2 faceted filter: dynamic Colour/Style/Size attribute facets in the Brand Filter Bar via new self-healing JPWBC_Attr_Index. Mirrors custom (non-taxonomy, API-synced) _product_attributes into plugin-owned jpwbc_af_{key} taxonomies (index on save + batched wp-cron backfill + Cache-tab Rebuild button + `wp jpwbc-attr-index rebuild`); per-brand facet dropdowns w/ counts (grouped SQL, version-namespaced cache), AND IN tax_query, faceted views noindexed. Widget: Show-attribute-filters + Limit-to-attributes allowlist. Full senior audit PASS (no C/H); applied all 3 MEDIUMs (object-cache-safe count version, double-index guard, collision-safe keys+allowlist-by-name). VERIFIED on staging 186999: discovered Size/Colour/Style, terms+counts created, batched cron advances (offset 150). REAL-DATA FINDING: raw values very messy (Style "DEFAULT"=121; "WHITE"/"1 WHITE"/"101 WHITE" fragment; sizes "0.60 MM"->"SMALL") -> normalisation map (jpwbc_af_value_label filter) needed for clean facets = Phase 2b, client decision. CI registered 1.14.0 OK | uploaded | patched |
| 1.13.0 | 2026-07-05 | clean | New "Brand Filter Bar" widget + [jpwbc_brand_filter] shortcode (Phase 1 of the faceted-filter client request): Category (clean combo URLs, indexable) + Price (?jpwbc_min_price/max -> _price meta clause on brand archive main query via pre_get_posts, AND-wrapped; cached per-brand price range) + Sort (?orderby, WC native ordering). Query-param based/cache-safe, no-JS friendly (sort auto-submits w/ JS); price/sort views noindex,follow (any non-default orderby too); clean brand+combo pages stay indexable. Hook+robots register only inside licensed feature block; GET sanitised at source. Full senior-reviewer audit PASS (no C/H; applied MEDIUM meta_query AND-wrap + broadened sort noindex). NOTE: colour/size facets deferred to Phase 2 — Birch's colour/style are custom (non-taxonomy) API-synced attrs; needs an indexer + normalisation. CI registered 1.13.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.12.0 | 2026-07-05 | clean | New "Brand Category Filter (chips)" Elementor widget + [jpwbc_brand_chips] shortcode: current brand's product categories as a Myer-style horizontal pill row via new JPWBC_Frontend::render_chips() (reuses resolve_current_brand + category_rows + combo URLs; skips dropdown single-render guard). Optional "All {brand}" chip + counts; active via jpwbc_cat query var. Full Style controls (typography/gap/padding/radius/border + Normal/Hover/Active colours), all {{WRAPPER}}-scoped. Full senior-reviewer audit PASS (no C/H/M; every sink escaped, {brand} label escaped post-substitution, no cross-brand leak, sanitize_title on brand input). CI registered 1.12.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.11.0 | 2026-07-05 | manual | All Brands (A-Z): "Brand list area padding" responsive DIMENSIONS control on Style tab -> .jpwbc-az-groups padding ({{WRAPPER}}-scoped selector; Elementor sanitises size/unit). Pure Elementor Style control - no PHP logic, output sinks or template change. Light diff-review (cosmetic, per audit-guide). CI registered 1.11.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.10.0 | 2026-07-05 | manual | All Brands (A-Z): "Gap below letter heading" responsive SLIDER (px/em) on Style tab -> .jpwbc-az-group__letter margin-bottom ({{WRAPPER}}-scoped selector; Elementor sanitises size/unit). Pure Elementor Style control - no PHP logic, output sinks or template change. Light diff-review (cosmetic, per audit-guide). CI registered 1.10.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.9.0 | 2026-07-05 | manual | All Brands (A-Z): "Sticky heading + alphabet bar" toggle (Myer-style) — wraps heading/search/index in .jpwbc-allbrands__head pinned via position:sticky while groups scroll; "Sticky top offset (px)" control emits --jpwbc-sticky-top CSS var (also drives jump scroll-margin). Offset (int)+esc_attr'd; static class names; non-sticky path adds only a plain block wrapper (verified div nesting/balance 4/4, no visual change). Light diff-review (display option + CSS + int-derived inline-style sink, per audit-guide). CI registered 1.9.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.8.0 | 2026-07-05 | manual | All Brands (A-Z): Style-tab controls for the "View all" link — link colour, hover colour + Group_Control_Typography — matching the existing heading/letters/brand-list controls. Elementor Style-controls only ({{WRAPPER}}-scoped selectors on .jpwbc-allbrands__viewall > a; no PHP logic/output sinks). Light diff-review (cosmetic exception per audit-guide). CI registered 1.8.0 OK; deployed to staging 186999 | uploaded | patched |
| 1.7.0 | 2026-07-04 | clean | All Brands (A-Z): new "Letters link to (Brands page URL)" option (index_url) so each A-Z letter links to a Brands page and jumps to that letter — works with brand lists hidden (menu/mega-menu use case). Per-letter section anchors made stable (jpwbc-az-b, was jpwbc-az-2-b) for reliable cross-page jumps. Senior-reviewer pre-release audit PASS (no C/H; index_url escaped esc_url_raw@boundary + esc_url/esc_attr@output, anchor fragment from fixed A-Z range; LOW: deliberate duplicate-id tradeoff if two A-Z widgets share a page, noted in upgrade notice; .pot 2 new strings deferred). CI registered 1.7.0 OK; deployed to staging 186999. Menu widget index_url to be set in Elementor editor (header tmpl 98113) | uploaded | patched |
| 1.6.0 | 2026-07-04 | manual | All Brands (A-Z): new "Alphabet layout" option (column grid vs inline boxed row, Myer-style). Value allowlisted grid\|inline in render + template, emitted only as a static CSS class; column-count control conditioned to grid; default grid unchanged. Incl. A-Z index text-align:left tweak. Light diff-review only (display option + CSS, no attack surface — per audit-guide cosmetic exception). CI registered 1.6.0 OK; staging 186999 updated + "All Brands" page (150706) shortcode set to index_layout=inline | uploaded | patched |
| 1.5.0 | 2026-06-26 | fixes (1 M) | All Brands (A-Z) full brands-directory options: 1-4 column brand lists, per-letter counts, live brand search; + link hover-colour control on both widgets. Audit PASS; fixed 1 M (per-letter count now skips un-linkable brands in get_brands_grouped so count==render). Deployed to staging 186999 | uploaded | patched |
| 1.4.0 | 2026-06-26 | manual | Typography group controls (font family+size+weight) on Trending + All Brands (A-Z) widgets (heading/letters/lists), replacing the size-only sliders; removed Trending/A-Z list bullets (list-style:none !important). Light diff-review only (Elementor Style-control + CSS, no attack surface, per audit-guide). Deployed to staging 186999 | uploaded | patched |
| 1.3.0 | 2026-06-26 | clean | All Brands (A-Z): column control (3/4/5/6) + show-brand-list toggle; Elementor Style controls (heading/list font size + colours) on both new widgets. Audit PASS (1 M: .pot header-only since scaffold, deferred - no wp-cli i18n). Deployed to staging 186999 | uploaded | patched |
| 1.2.0 | 2026-06-26 | clean | New Trending Brands (click-tracked via nonce'd AJAX + term meta; 1h cache; fallback so never blank) and All Brands (A-Z) (alphabet jump-index + per-letter grouped sections) widgets/shortcodes; shared on-demand asset enqueue; audit PASS (A-Z query cached per finding). CI registered version OK this time; deployed to staging 186999 | uploaded | patched |
| 1.1.0 | 2026-06-26 | clean | New Style tab: colour pickers (active/chevron/accent) output as CSS variables; hex-validated, idempotent save preserved. CI upload step reported success but did NOT register the version — caught by verifying the server, uploaded manually with --ver. Deployed to staging 186999 | uploaded | patched |
| 1.0.6 | 2026-06-26 | manual | Cosmetic CSS: brand toggle button ignores theme padding/border (!important); light diff-review only (no agent, per audit-guide cosmetic exception); deployed to staging 186999 | uploaded | patched |
| 1.0.5 | 2026-06-26 | clean | Remove "Other brands clickable" toggle; all brands expandable by default (built-in); deployed + verified on staging 186999 | uploaded | patched |
| 1.0.4 | 2026-06-26 | clean | REAL settings-save fix: non-idempotent per-tab sanitiser early-returned on its 2nd pass and reverted saves (latent since 1.0.0). Merge moved to handler, sanitiser made idempotent; root cause proven via live POST/sanitise capture on staging 186999; save persistence verified | uploaded | patched |
| 1.0.3 | 2026-06-26 | clean | Settings-save attempt: active tab sent as top-level field (rule out WAF stripping of underscore-prefixed POST keys) — did not resolve; superseded by 1.0.4 | uploaded | patched |
| 1.0.2 | 2026-06-19 | clean | Settings save via admin-post.php (bypass options.php allowed_options whitelist that role/security plugins filter) + "Settings saved" notice; deployed to staging 186999 | uploaded | patched |
| 1.0.1 | 2026-06-19 | clean | Hotfix: site-wide front-end fatal (canonical filter TypeError under strict_types) when Rank Math active; verified on staging site 186999 | uploaded | patched |
| 1.0.0 | 2026-06-19 | fixes (1 H) | Initial release: in-brand category dropdown + clean /brands/{brand}/{category}/ URLs + Rank Math SEO for WooCommerce brand archives | uploaded | patched |
